% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xtpcmg.R
\name{xtpcmg}
\alias{xtpcmg}
\title{Panel Cointegrating Polynomial Regressions via FM-OLS}
\usage{
xtpcmg(
  data,
  y,
  x,
  panel_id,
  time_id,
  model = c("mg", "pmg"),
  q = 2L,
  controls = NULL,
  trend = 1L,
  kernel = "ba",
  bw = "And91",
  effects = "oneway",
  corr_rob = FALSE
)
}
\arguments{
\item{data}{A data frame in long format.}

\item{y}{Character. Name of the dependent variable.}

\item{x}{Character. Name of the polynomial (I(1)) regressor.}

\item{panel_id}{Character. Name of the panel identifier variable.}

\item{time_id}{Character. Name of the time variable.}

\item{model}{Character. Estimator: \code{"mg"} for Group-Mean FM-OLS
(default) or \code{"pmg"} for Pooled FM-OLS.}

\item{q}{Integer. Polynomial degree: \code{2} (quadratic, default) or
\code{3} (cubic).}

\item{controls}{Character vector. Names of additional I(1) control
variables. Default is \code{NULL} (no controls).}

\item{trend}{Integer. Deterministic trend type: \code{1} for demeaning
only (default), \code{2} for demeaning and detrending.}

\item{kernel}{Character. HAC kernel: \code{"ba"} (Bartlett, default),
\code{"pa"} (Parzen), \code{"qs"} (Quadratic Spectral),
\code{"tr"} (truncated), \code{"bo"} (Bohman).}

\item{bw}{Character or numeric. Bandwidth for HAC estimation. \code{"And91"}
(default) uses Andrews (1991) automatic selection. A numeric value sets
the bandwidth directly.}

\item{effects}{Character. For Pooled FM-OLS: \code{"oneway"} (default)
for one-way demeaning or \code{"twoways"} for two-way demeaning.}

\item{corr_rob}{Logical. For Group-Mean FM-OLS: if \code{TRUE}, use
cross-sectionally robust VCV. Default is \code{FALSE}.}
}
\value{
An object of class \code{"xtpcmg"} with elements:
  \code{coefficients} (named numeric vector),
  \code{vcov} (variance-covariance matrix),
  \code{se} (standard errors),
  \code{tstat} (t-statistics),
  \code{pvalue} (two-sided p-values),
  \code{model} (estimator type: \code{mg} or \code{pmg}),
  \code{q} (polynomial degree),
  \code{N} (number of panel units),
  \code{TT} (number of time periods),
  \code{y} (dependent variable name),
  \code{x} (polynomial variable name),
  \code{tp} (turning point estimate, quadratic models only),
  \code{tp_se} (delta-method SE for turning point),
  \code{tp_lo} and \code{tp_hi} (95\% CI bounds),
  \code{ind_coef} (individual FM-OLS estimates, MG model),
  \code{swamy_s} and \code{swamy_p} (Swamy slope homogeneity test).
}
\description{
Estimates a polynomial cointegrating regression in a panel using either
Group-Mean FM-OLS (Wagner & Reichold 2023) or Pooled FM-OLS (de Jong &
Wagner 2022). Models the long-run relationship:
}
\details{
\deqn{y_{it} = \alpha_i + \beta_1 x_{it} + \beta_2 x_{it}^2 [+ \beta_3 x_{it}^3]
       [+ \gamma z_{it}] + u_{it}}

where \eqn{x_{it}} and \eqn{z_{it}} are I(1) processes.
}
\examples{
dat <- grunfeld_cmg()

# Group-Mean FM-OLS (quadratic EKC-type model)
res <- xtpcmg(dat, y = "invest", x = "mvalue",
              panel_id = "firm", time_id = "year",
              model = "mg", q = 2L)
print(res)
summary(res)

# Pooled FM-OLS
res2 <- xtpcmg(dat, y = "invest", x = "mvalue",
               panel_id = "firm", time_id = "year",
               model = "pmg", q = 2L)
print(res2)

}
\references{
Andrews, D.W.K. (1991). Heteroskedasticity and autocorrelation consistent
covariance matrix estimation. \emph{Econometrica}, 59(3), 817--858.
\doi{10.2307/2938229}

de Jong, R.M. and Wagner, M. (2022). Panel cointegrating polynomial
regressions. \emph{Annals of Applied Statistics}, 16(1), 416--442.
\doi{10.1214/21-AOAS1536}

Wagner, M. and Reichold, K. (2023). Panel cointegrating polynomial
regressions. \emph{Econometric Reviews}, 42(9--10), 782--827.
\doi{10.1080/07474938.2023.2178141}
}
